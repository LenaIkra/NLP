# Классификация приоритетов сообщений (Telegram-тикеты)

Проект в рамках курса NLP.  
Задача — классифицировать сообщения (тикеты поддержки) по приоритету: **low / medium / high**.


📘 Описание задачи

Система должна обрабатывать тексты на английском и немецком языках, выполнять предобработку, векторизацию и классификацию сообщений с использованием традиционных ML-методов и нейронных сетей без применения трансформеров.

Обучение моделей производилось отдельно только на английском и английском и немецком языках. Изначально идея была делать только на английском языке, но обЪем датасета после фильтрации по языку (vocab size = 5130.0) практически похоронил обучение нейросетей, поэтому обучение произвели еще раз на смеси английского и немецкого языков.


# Обучение на английском
---

## 1. Датасет

**Источник:** [Customer Support Tickets Dataset](https://www.kaggle.com/datasets/tobiasbueck/multilingual-customer-support-tickets)  
Использованные файлы: dataset-tickets-multi-lang-5-2-50-version.csv

**Характеристики:**
- Размер после фильтрации языка: ~16338 записей
- Поля: `subject`, `body`, `priority`, `language`
- Использовано только: **английские тексты**

**Качество данных:**
- Доля шумных записей (короткие/пустые): ~5-7%
- Были удалены неанглийские записи

---

## 2. Предобработка данных

Шаги:
1. Очистка текста (регулярные выражения, нижний регистр)
2. Лемматизация (spaCy, `en_core_web_sm`)
3. Удаление стоп-слов (NLTK stopwords)
4. Объединение полей: `text_final = subject_final + " " + body_final`
5. Разделение train/test (80/20)

---

## 3. Методы классификации

Были протестированы следующие baseline модели:
LR, NB(Multinomial), SVM, FastText.

Векторизаторы: BOW(от 1 до 5 gram), TF-IDF(от 1 до 5 gram).

Из нейросетей протестированы: TwoLayerBiLSTM, CNN+BiLSTM.

### 🧾 Результаты классификации текстовых сообщений

| Rank | Model                          | Vectorizer                | Accuracy | Precision | Recall | F1     |
|------|---------------------------------|---------------------------|-----------|------------|---------|--------|
| 1 | **SVM** | TF-IDF (1-4gram) | **0.7742** | 0.7774 | 0.7742 | 0.7729 |
| 2 | **SVM** | BoW (1-4gram) | 0.7638 | 0.7655 | 0.7638 | 0.7619 |
| 3 | **Logistic Regression** | BoW (1-4gram) | 0.7491 | 0.7509 | 0.7491 | 0.7476 |
| 4 | Logistic Regression | BoW (1-3gram) | 0.7424 | 0.7427 | 0.7424 | 0.7410 |
| 5 | **fastText** | FastText embeddings | 0.7417 | 0.7448 | 0.7417 | 0.7397 |
| 6 | Logistic Regression | BoW (1-2gram) | 0.7160 | 0.7154 | 0.7160 | 0.7152 |
| 7 | Logistic Regression | TF-IDF (1-4gram) | 0.7038 | 0.7032 | 0.7038 | 0.7032 |
| 8 | MultinomialNB | BoW (1-3gram) | 0.6922 | 0.7194 | 0.6922 | 0.6822 |
| 9 | Logistic Regression | TF-IDF (1-3gram) | 0.6882 | 0.6880 | 0.6882 | 0.6880 |
| 10 | Logistic Regression | TF-IDF (1-2gram) | 0.6478 | 0.6494 | 0.6478 | 0.6482 |
| 11 | MultinomialNB | BoW (1-2gram) | 0.6291 | 0.6403 | 0.6291 | 0.6196 |
| 12 | **TwoLayer BiLSTM** | — | 0.5884 | — | — | — |
| 13 | MultinomialNB | TF-IDF (1-3gram) | 0.5838 | 0.6812 | 0.5838 | 0.5141 |
| 14 | MultinomialNB | TF-IDF (1-2gram) | 0.5520 | 0.6544 | 0.5520 | 0.4869 |
| 15 | **CNN+BiLSTM** | — | 0.5508 | — | — | — |

---

### 📊 Ключевые выводы

- 🔝 **SVM (TF-IDF 1–4gram)** показала наилучший результат: **Accuracy = 0.774, F1 = 0.773.  



---

## 4. Анализ ошибок

- Наихудшие результаты у **Naive Bayes**  с unigram - из бэйзлайн моделей и нейросетей
- Класс **low** путается чаще всего — модель склонна относить его к medium  
- Очень низкий скор у нейросетей - данных для обучения недостаточно, после этого решено было попробовать обучить модели на двух языках и посмотреть разницу.


---

## 5. Ссылки

- 📓 [Colab Notebook - английский язык](https://colab.research.google.com/drive/1KqnSehrrTdQ5lDBUu2yDNerp3LBuUSOT?usp=sharing)  

---

# Обучение на английском и немецком языках

## 1. Датасет
📂 Описание датасета

**Источник:** [Customer Support Tickets Dataset](https://www.kaggle.com/datasets/tobiasbueck/multilingual-customer-support-tickets) 

**Использованные файлы:** 

    •	dataset-tickets-multi-lang-4-20k.csv
	  •	dataset-tickets-multi-lang3-4k.csv
	  •	dataset-tickets-multi-lang-5-2-50-version.csv
	  •	dataset-tickets-german_normalized.csv
	  •	dataset-tickets-german_normalized_50_5_2.csv

**Общий размер:** ≈ 35 000 сообщений.

**Языки:** английский (en), немецкий (de) - основные.

**Классы приоритета:** high, medium, low. (также встречались редкие, их доля порядка 2%- critical, very low - удалены для чистоты эксперимента и адекватного сравнения с вариантом только на английском языке).


**Покрытие и структура:**

	•	subject — тема обращения,
	•	body — основное сообщение,
	•	language — язык текста,
	•	priority — целевая переменная.

**Качество данных:**

	•	Дубликаты и пустые тексты удалены.
	•	Синтаксис унифицирован до нижнего регистра,
	•	Символы, не относящиеся к латинице и немецким буквам, очищены.
	•	После фильтрации редких классов — ~30 000 записей.

**Технические параметры:**

	•	Данные сбалансированы по языкам и классам,
	•	доля «шумных» (неинформативных) сообщений ≈ 5–7%,
	•	кодировка UTF-8, текст очищен и лемматизирован.

⸻

## 2. Предобработка данных

Предобработка реализована классом TextPreprocessor:
	•	очистка от HTML-тегов, цифр и пунктуации,
	•	токенизация и лемматизация (модели spacy.en_core_web_sm, spacy.de_core_news_sm),
	•	удаление стоп-слов (nltk.corpus.stopwords),
	•	объединение subject + body в итоговое поле text_final.

**Результат:** тексты унифицированы по структуре и длине, сохранены в кэш artifacts/df_preprocessed.parquet.
Всё повторное обучение использует кешированные файлы, что ускоряет работу в Kaggle Runtime.

⸻

## 3. Методы классификации

🧩 **Используемые модели:**

	1.	Классические ML-методы:
	•	Logistic Regression, SVM, Naive Bayes, XGBoost
	•	TF-IDF и Bag-of-Words признаковые матрицы
	•	GridSearchCV для подбора гиперпараметров SVM (C, class_weight, min_df, max_df)
	2.	Нейросетевые архитектуры (PyTorch):
	•	TextCNN
	•	BiLSTM
	•	Attn-BiLSTM
	•	FastText + CNN / BiLSTM / BiLSTM + Attention
Все модели реализованы в виде классов (nn.Module), используют кэшированные матрицы FastText-векторов.


## 📊 Результаты классификации сообщений

| Rank | Model                            | Vectorizer                  | Accuracy | Precision | Recall | F1     |
|------|-----------------------------------|------------------------------|-----------|------------|---------|--------|
| 1 | **SVM (GridSearchCV)** | TF-IDF (1–4gram, tuned) | **0.7574** | 0.7589 | 0.7574 | **0.7564** |
| 2 | **SVM** | TF-IDF (1–4gram) | 0.7463 | 0.7501 | 0.7463 | 0.7447 |
| 3 | **Logistic Regression** | BoW (1–4gram) | 0.7355 | 0.7387 | 0.7355 | 0.7338 |
| 4 | SVM+LR+NB (Ensemble, Grid) | TF-IDF (word+char) | 0.7026 | 0.7132 | 0.7026 | 0.6980 |
| 5 | FastText+CNN (trainable) | text_nn | 0.6944 | 0.6945 | 0.6944 | 0.6938 |
| 6 | TextCNN | text_nn | 0.6792 | 0.6789 | 0.6792 | 0.6790 |
| 7 | MultinomialNB | BoW (1–3gram) | 0.6837 | 0.6957 | 0.6837 | 0.6749 |
| 8 | FastText+CNN (pretrained, freeze) | text_nn | 0.6743 | 0.6744 | 0.6743 | 0.6742 |
| 9 | FastText+BiLSTM+Attn (trainable) | text_nn | 0.6497 | 0.6521 | 0.6497 | 0.6501 |
| 10 | XGBoost | TF-IDF (1–3gram) | 0.6011 | 0.6443 | 0.6011 | 0.5869 |
| 11 | BiLSTM | text_nn | 0.5779 | 0.5847 | 0.5779 | 0.5789 |
| 12 | FastText+BiLSTM+Attn | text_nn | 0.5627 | 0.5758 | 0.5627 | 0.5546 |
| 13 | AttnBiLSTM (pretrained, freeze) | text_nn | 0.5639 | – | – | – |

---

## 🧠 Выводы

1. **SVM (GridSearchCV, TF-IDF 1–4gram)** показала наилучшее качество:  
   **Accuracy = 0.7574, F1 = 0.7564**, опередив все остальные модели.  
   Улучшение относительно базовой SVM составило ~1%, благодаря подбору гиперпараметров `C`, `loss`, `class_weight`.  SVM хорошо работает на TF-IDF-векторах и требует меньше вычислений, чем нейросети.

2. **Классические модели (SVM, Logistic Regression)** демонстрируют высокую эффективность при обработке коротких сообщений с разными структурами языка (EN/DE).  

3. **FastText+CNN (trainable)** занял 5-е место с F1≈0.69, показав, что  дообучаемые эмбеддинги улучшают нейросетевые модели по сравнению с замороженными.

4. **BiLSTM и Attention-модели** дают более слабые результаты (~0.56–0.58 F1), но лучше захватывают контекст — их потенциал выше на больших корпусах данных.

5. **Ансамбль (SVM+LR+NB)** обеспечивает более стабильные предсказания,  однако прирост точности незначителен из-за схожести признаков у базовых моделей.

---

## ⚙️ Подбор параметров (GridSearchCV)

Для SVM применён `GridSearchCV`, настроенный на параметры:


param_grid = 

    'C': [0.5, 1, 2, 3, 5],
    'loss': ['hinge', 'squared_hinge'],
    'class_weight': [None, 'balanced']



## 4. Анализ ошибок

	•	Основные ошибки наблюдаются между смежными классами приоритета (high ↔ medium, medium ↔ low), что естественно из-за размытых границ и субъективной разметки.
	•	На немецких тикетах модели ошибаются чаще, чем на английских, из-за меньшего объёма данных и большей морфологической сложности.
	•	Модели путают обращения, где слова вроде “urgent”, “now”, “bitte sofort” отсутствуют, и не могут достоверно определить высокий приоритет по контексту.
⸻

## 5. Ссылки

[Kaggle notebook](https://www.kaggle.com/code/elenaikrina/nlp-en-de-final)

⸻
