# NLP
Здесь будут размещаться ДЗ и проекты по дисциплине "NLP".

[Отчет по ДЗ_1](https://github.com/LenaIkra/NLP/tree/2e2cfbeafdb0cf5fa63ea355eb413bff7140f376/Text%20classification)

[Отчет по ДЗ_2 (Финальный проект)](## 🏁 Финальные результаты классификации текстов)

# Команда №8
Участники:
1. Бабухина Е.М.
2. Миронов А.В.

# Тема
Классификация писем по приоритету: Определение приоритета письма

# Датасет
[Customer IT Support - Ticket Dataset](https://www.kaggle.com/datasets/tobiasbueck/multilingual-customer-support-tickets)
---

## 1. Общая информация
	•	Источник: Kaggle 
	•	Тематика: тикеты обслуживания клиентов, запросы в службу поддержки, классификация по приоритету/urgency, очереди, типы запросов
	•	Языки: английский (EN), немецкий (DE), ИСП., ФР., ПТ.

---
## 2. Состав и структура данных

| Поле / Атрибут | Описание |
|----------------|-----------|
| **subject**    | Заголовок запроса клиента (короткая строка) |
| **body**       | Основной текст запроса / описание проблемы |
| **priority**   | Категориальная метка срочности / важности (например, low / normal / high / critical) |
| **queue**      | Очередь или отдел поддержки (Billing, Technical Support, General и т.д.) |
| **type**       | Тип тикета (request, incident, inquiry и др.) |
| **language**   | Язык, на котором написан тикет (например, английский, немецкий, французский и т.п.) |
| **tags / labels** | Дополнительные метки, связанные с тикетом (например, "payment", "login issue") |
| **answer**     | Ответ службы поддержки (если присутствует) |

---

## 3. Размеры, сплиты, покрытия

| Параметр              | Описание |
|-----------------------|-----------|
| Общее число записей   | ~67 000 |
| Сплиты                | Разбиение на train/val/test нужно делать самостоятельно |
| Эффективное покрытие  | Тикеты на разных языках, разные очереди и приоритеты; позволяет обучать мультилингвальные и мультитематические модели |

---

## 4. Качество данных

| Характеристика          | Описание |
|-------------------------|-----------|
| Чистота / нормализация  | Тексты содержат заголовки и тела; возможны пропуски (`null`-значения); встречаются спецсимволы, HTML-теги, URL, email-адреса; есть вариативность правописания и опечатки |
| Объективное качество    | - Распределение приоритетов может быть неравномерным (low / normal / high)<br>- Есть как короткие тикеты (одно предложение), так и длинные описания |
| Доля «шумных» сегментов | - Пропущенные поля (пустые subject/body)<br>- Форматирование, смешанный язык, шаблонные фразы<br>- ~5–15% записей нуждаются в значительной чистке |

---

## 5. «Транскрипция» / Языковая надёжность

| Параметр       | Описание |
|----------------|-----------|
| Тип данных     | Письменные тексты (нет звуковых записей, транскрипции отсутствуют) |
| Языковое покрытие | Разные языки, возможны смешанные тикеты (например, английский с вставками на другом языке) |
| Правописание   | В целом нормальное, но встречаются опечатки, неформальные конструкции и разговорный стиль |

## Ноутбук на Kaggle с моделями

[Ноутбук](https://www.kaggle.com/code/elenaikrina/nlp-en-final-with-bert)

## 🏁 Финальные результаты классификации текстов

| Rank | Model | Vectorizer | Accuracy | Precision | Recall | F1 |
|:--:|:--|:--|:--:|:--:|:--:|:--:|
| **1** | **SVM (GridSearchCV)** | TF-IDF (1–4 gram, tuned) | **0.7432** | **0.7446** | **0.7432** | **0.7414** |
| 2 | SVM | TF-IDF (1–4 gram) | 0.7342 | 0.7403 | 0.7342 | 0.7308 |
| 3 | Logistic Regression | BoW (1–4 gram) | 0.7140 | 0.7177 | 0.7140 | 0.7105 |
| 4 | **BERT (fine-tuned)** | transformer | 0.7021 | 0.7040 | 0.7021 | 0.7013 |
| 5 | MultinomialNB | BoW (1–4 gram) | 0.6881 | 0.7240 | 0.6881 | 0.6739 |
| 6 | TextCNN (trainable) | text_nn | 0.6623 | 0.6615 | 0.6623 | 0.6605 |
| 7 | FastText + CNN (trainable) | text_nn | 0.6605 | 0.6599 | 0.6605 | 0.6602 |
| 8 | TextCNN (pretrained, freeze) | text_nn | 0.6344 | 0.6361 | 0.6344 | 0.6349 |
| 9 | FastText + CNN (pretrained, freeze) | text_nn | 0.6292 | 0.6424 | 0.6292 | 0.6284 |
| 10 | FastText + BiLSTM + Attn (trainable) | text_nn | 0.5793 | 0.5837 | 0.5793 | 0.5808 |
| 11 | BiLSTM (trainable) | text_nn | 0.5705 | 0.5779 | 0.5705 | 0.5724 |
| 12 | BERT (fine-tuned, 12 epochs) | transformers | 0.5412 | 0.5429 | 0.5412 | 0.5269 |
| 13 | FastText + BiLSTM + Attn (pretrained, freeze) | text_nn | 0.4528 | 0.4639 | 0.4528 | 0.4524 |
| 14 | BiLSTM (pretrained, freeze) | text_nn | 0.4520 | 0.4917 | 0.4520 | — |

---

## 📊 Анализ результатов

1. **Лучшие результаты** показала модель **SVM с TF-IDF (1–4 грамм)**,  
   где точность достигла **≈0.74 F1**, что подтверждает эффективность классических методов при ограниченном количестве данных.

2. **BERT (fine-tuned)** показал **F1 ≈ 0.70** —  
   это высокий результат для небольшой обучающей выборки (~60k строк),  
   однако без расширенного fine-tuning и увеличения числа эпох модель начала **переобучаться** после 6–8 эпох (рост `val_loss`).

3. **Нейросетевые архитектуры (BiLSTM, TextCNN, FastText+CNN)**  
   показали стабильные результаты **F1 = 0.57–0.66**.  
   Обучаемые эмбеддинги (`trainable=True`) дали **прирост до +0.05–0.07 F1**,  
   по сравнению с замороженными (`freeze=True`).

4. **FastText + BiLSTM + Attention** и **BiLSTM (pretrained)** продемонстрировали наименьшую точность.  
   Это связано с тем, что датасет содержит короткие тексты и мало контекста,  
   а модели с рекуррентными слоями эффективнее работают с длинными последовательностями.

5. **Наиболее сбалансированные результаты по всем метрикам (accuracy, precision, recall, F1)**  
   получены у моделей TF-IDF + SVM/LogReg — благодаря стабильным признаковым векторам и хорошей регуляризации.

---
### 🔍 Error Analysis

Для итоговой модели проведён анализ ошибочных классификаций.

Основные наблюдения:

1. **Ошибки чаще встречаются для класса “medium”** — он пересекается по лексике и с “low”, и с “high”.  
   Например, тексты с нейтральными словами (“please”, “as soon as possible”) классифицируются неоднозначно.
2. **Короткие тексты (1–2 слова)** плохо распознаются всеми моделями — не хватает контекста для уверенного предсказания.
3. **SVM и логистическая регрессия** чувствительны к несбалансированности классов — помог параметр `class_weight='balanced'`.
4. **Нейросетевые модели (CNN, BiLSTM)** дают лучшие результаты на длинных текстах,  
   но переобучаются на редких паттернах при малом числе примеров.
5. **BERT** лучше понимает контекст, но требует больше эпох для стабилизации (`validation_loss` начинает расти после 7–8 эпохБ то есть модель перестает учиться).


## 📈 Выводы

- Для данного датасета (английские обращения клиентов) классические ML-модели остаются **лидерами по обобщающей способности**, особенно с TF-IDF признаками.  
- Нейронные сети требуют большего количества данных и более глубокого fine-tuning.  
- Трансформер (BERT) продемонстрировал потенциал, но нуждается в дополнительных эпохах и регуляризации.  
- Замороженные эмбеддинги снижают качество, так как не адаптируются под конкретную задачу классификации.

---

## 🧠 Общие рекомендации

| Подход | Преимущества | Недостатки |
|:--|:--|:--|
| **TF-IDF + SVM** | высокая точность, устойчивость, простота | требует ручного подбора n-грамм и параметров |
| **FastText + CNN** | обучается быстро, устойчив к шуму | хуже для коротких фраз |
| **BERT** | контекстное понимание текста | требует больше данных и GPU |

---

📦 **Вывод:**  
Для применения в продакшене на данном этапе рекомендуется **SVM (GridSearchCV) с TF-IDF**,  
а **BERT** можно оставить в качестве перспективного направления для расширенного fine-tuning и дообучения на реальных данных.

